Debugging Prompts
I have a React useEffect that fetches featured OMDb movie results on mount. Review this effect and tell me if any dependency can cause repeated API calls or an infinite loop.
My IntersectionObserver infinite scroll sometimes fires twice quickly. What guard conditions should I add around loading, hasMore, and page state?
In a Vite app, why would import.meta.env.VITE_OMDB_KEY be undefined even though I created a .env file?
Architecture Help Prompts
Suggest a clean folder structure for a React movie discovery SPA that uses reusable components, custom hooks, API services, and Context API for favorites.
Review this FavoritesContext implementation. Is the localStorage sync safe, and how can I avoid unnecessary re-renders?
How should I separate OMDb API logic from React components when using Axios in a Vite project?
Optimization Prompts
What are lightweight performance improvements for a movie grid that loads poster images from OMDb?
Check this custom debounce hook. Does it correctly wait 500ms after typing stops before updating the debounced value?
What should I watch for when appending paginated API results in React without duplicating movie cards?
